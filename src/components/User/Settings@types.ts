export type GeneralSettingsStateTypes = {
    openLanguageSelect: boolean;
    selectedLanguage: string | null;
};

export type ProfileSettingsStateTypes = {
    passwordIsIncorrect: boolean;
    verificationEmailSend: boolean;
};
